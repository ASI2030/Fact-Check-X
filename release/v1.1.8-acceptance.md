# Fact-Check-X v1.1.8 acceptance contract

1. DeepSeek and Yuanbao references are not treated as verified source text until the referenced page has been opened and its body has been captured. The stored result distinguishes source body, answer context, and acquisition failure, and the Xu Jianjun v1.1.7 sample no longer reports real DeepSeek links as self-echoed pseudo-citations. Numbered list items and citations in later answer paragraphs cannot be rebound as local evidence for an earlier claim.
2. Every stage emits an independently openable report and a machine-enforced checkpoint. In interactive mode, the next stage rejects execution until the preceding checkpoint has been explicitly acknowledged; full-auto mode still emits all four checkpoints in order.
3. Stage 3 owns the authoritative answer, authoritative evidence, and evidence gaps. Stage 4 consumes the immutable Stage-3 result and owns platform scoring and per-platform verdict detail without recomputing or duplicating Stage-3 analysis.
4. The four HTML reports share one navigation, typography, spacing, color, and evidence presentation system. Direct answers and supplemental references are visually distinct; authoritative evidence is displayed in a readable single column.
5. The frozen report-package replay, collector and pipeline regression suites, clean-package tests, and one real browser end-to-end run all pass before any v1.1.8 publication or external announcement.

Anything outside these five checks is deferred unless it blocks them.
